# ── 必须安装 ──────────────────────────────────────────────
openai>=1.30.0          # OpenAI / DeepSeek 兼容接口
pandas>=2.2.0           # CSV 读取与处理
python-dotenv>=1.0.0    # .env 文件加载

# ── 可选：使用 Claude 模型时安装 ──────────────────────────
# anthropic>=0.28.0
